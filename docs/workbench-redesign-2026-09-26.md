# 工作台布局重构 · 2026-09-26

目标：采用 Codex 的单侧栏、安静对话画布、底部输入器和按需检查面板的信息层级。保留 Pig Agent 身份与现有执行、审批、账户、文件权限。不是复制商标或伪装为 Codex。

优先问题与验收：
- 输入区重复专家选择与配置：合并为一次选择，调试归入高级选项；新任务主操作清楚。
- 导航和任务标题占用过多高度：单侧栏，桌面可折叠，手机抽屉；顶部高度 56px。
- 能力库按钮与大卡片过密：统一紧凑内容列表、搜索及详情；导入按需展开。
- 设置缺少层级：分类导航与内容分栏，手机横向分类；现有保存行为不变。
- 过程与最终结果混杂：保留逐轮过程折叠、审批卡片与最终回答；文件按需展示。

实施与验证：保留改版前截图；用隔离模拟接口验证 1440、390、320px 的空态、两轮、执行、审批、错误、设置和能力库。验证输入选项、导航、详情开关及设置保存；截图自审后修正。不消耗模型额度，不读取生产用户数据。

状态：本地实现与界面验收完成。源码范围为 Web 共享界面和云端工作台，不修改调度与鉴权逻辑。

## 交付与证据

已完成：单侧栏及桌面收起/展开；56px 任务栏；对话与输入区重排；项目选择并入输入器，去除重复专家选择；调试开关收进开发者选项；能力库改紧凑列表、导入按需展开及错误重试；设置分类与内容分栏；项目、自动化统一密度。移除原 688 行 `joydesk-workbench.css`，将资源详情样式与工作台布局分别集中管理。

视觉复核修复了新任务重复标题、能力按钮图标换行和 320px 工具栏重叠。手机配置与发送分两行，快捷任务入口为两列。保留审批、文件、运行事件、技能导入及账户保存处理函数。

验证结果：
- Web 31 个测试文件、303 项测试通过；架构检查、Web 类型检查与构建通过。仍有原有主包超过 500kB 的构建提示。
- 浏览器脚本 18 场景 × 3 尺寸（1440×900、390×844、320×844），另含详情截图，共 61 张；无页面异常。
- 检查侧栏收起/恢复、专家选取、调试按需展开、工具栏无重叠、技能/专家详情、项目文件页、右侧成果面板、设置保存后刷新保持，以及原有上下文弹层与逐轮对话回归。
- 额外通过应用内真实浏览器点击技能列表和详情，并检查菜单渲染。
- 本地证据：`data/workbench-redesign-20260926/before/`、`after/` 与 `comparison.html`，不提交 Git。对照页仅列两版都有的同尺寸场景。

复现：

```sh
pnpm --filter @pig-agent/web build
PIG_SMOKE_DATA_DIR=/tmp/pig-layout-check node scripts/smoke-conversation-experience.mjs
# 保持一个可交互的隔离界面预览（Ctrl-C 退出）：
PIG_SMOKE_PREVIEW=1 PIG_SMOKE_DATA_DIR=/tmp/pig-layout-preview node scripts/smoke-conversation-experience.mjs
```

预览地址 `http://127.0.0.1:18792/#/conversations`，明确标记模拟数据。端口 18791/18792 必须空闲。预览不连接线上账号，设置仅存在模拟服务内，不执行真实任务。

本次未部署生产；未改管理后台或 Electron 原生窗口，本地模式仅共享对话样式调整。没有把界面模拟验证宣称为新的 Runner、模型或集群验收。
